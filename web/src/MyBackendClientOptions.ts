/** Provides options for MyBackendClient. */
export default interface MyBackendClientOptions {
	/** MyBackend service Uri. */
	serviceUri: string;

	/** Requests localization culture. */
	culture?: string;
}
